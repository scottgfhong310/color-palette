# color-palette — 環境變數範本
# 複製成 .env（已 .gitignore，永不進版控）。所有變數皆「選配」——不設也能跑，只是少了 LLM 潤稿。

# 選配：色彩肖像的「LLM 潤稿」（把決定論的一句描述用 Claude 改寫得更漂亮）。
# 未設 → 功能靜默停用（明細裡不出現潤稿鈕）；純核心（color-portrait-lib）仍完全決定論、零相依。
# 取得金鑰：https://console.anthropic.com/  （x-api-key）
# ANTHROPIC_API_KEY=sk-ant-...

# 選配：潤稿用的模型（預設 claude-opus-4-8）。這是短句改寫的小任務，
# 想省成本可改 claude-haiku-4-5。
# ANTHROPIC_MODEL=claude-opus-4-8

# 選配：覆寫預設 port（canon 預設 3000）。
# PORT=3000
